sensor:
  - platform: command_line
    name: ipm_inventory_products
    unique_id: ipm_inventory_products
    command: "python3 /config/PaddiSense/Scripts/ipm_read.py read"
    scan_interval: 300
    value_template: "{{ value_json.total_products | default(0) }}"
    json_attributes:
      - products

